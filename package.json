{
  "name": "home-server",
  "version": "0.1.0",
  "description": "MY private home server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "suncalc": "^1.7.0",
    "webdriver-sync": "^0.18.0"
  },
  "dependencies": {
    "dropbox": "^2.0.0",
    "node-dropbox": "^0.1.7"
  }
}
